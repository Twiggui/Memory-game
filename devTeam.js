
/*const selectImage = document.querySelectorAll("img");
const selectLi = document.querySelectorAll(".card");



function changeBorder(i){
    selectLi[i].style.borderColor = "gold";
}

function resetBorder(i){
    selectLi[i].style.borderColor = "darkgray";
}

for (let i=0 ; i<selectImage.length ; i++){

    selectImage[i].addEventListener("mouseover", changeBorder[i]);
    selectImage[i].addEventListener("mouseout", resetBorder[i]);
}
*/

